##Set Node Js Version
machine:
  node:
    version: 6.8.1
    
# Add some environment variables
  environment:
    CIRCLE_ENV: dev
    
## Customize dependencies
dependencies:

  override:
    - npm install

test:
  override:
    - npm run test
